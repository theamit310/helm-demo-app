apiVersion: v1
kind: Service
metadata:
  name: {{ include "Samlpe-Helm-ArgoCD.fullname" . }}
  labels:
    {{- include "Samlpe-Helm-ArgoCD.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Samlpe-Helm-ArgoCD.selectorLabels" . | nindent 4 }}
