apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Samlpe-Helm-ArgoCD.fullname" . }}-test-connection"
  labels:
    {{- include "Samlpe-Helm-ArgoCD.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Samlpe-Helm-ArgoCD.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
